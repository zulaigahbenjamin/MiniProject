import { createStore } from 'vuex'
import HomeView from '../views/HomeView.vue'
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
